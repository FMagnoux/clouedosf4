<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Clouedo{% endblock %}</title>
    {% block stylesheets %}{% endblock %}
    {% block head_css %}
        <link href="//maxcdn.bootstrapcdn.com/bootstrap/3.3.0/css/bootstrap.min.css" rel="stylesheet" id="bootstrap-css">
        <link rel="stylesheet" href="{{ asset('css/navbar.css') }}">
        <link rel="stylesheet" href="{{ asset('css/clouedo.css') }}">
        <link rel="stylesheet" href="{{ asset('css/form.css') }}">
    {% endblock %}
    {% block head_js %}
        <script src="//maxcdn.bootstrapcdn.com/bootstrap/3.3.0/js/bootstrap.min.js"></script>
        <script src="//code.jquery.com/jquery-1.11.1.min.js"></script>

    {% endblock %}

</head>
<body>
{% block nav %}
    <div class="">
        <nav class="navbar navbar-inverse">
            <div class="container">
                <div class="navbar-header">
                    <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-collapse-4">
                        <span class="sr-only">Toggle navigation</span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                    </button>
                    <a class="navbar-brand" href="{{ path('app_home') }}">Clouedo</a>
                </div>
                <div class="collapse navbar-collapse" id="navbar-collapse-4">
                    <ul class="nav navbar-nav navbar-right">
                        {% if app.user %}
                            <li><a href="{{ path('app_space_show', { 'id': app.user.space.id }) }}"><span class="glyphicon glyphicon-cloud"></span> Espace</a></li>
                            <li><a href="{{ path('app_profil') }}"><span class="glyphicon glyphicon-user"></span> Profil</a></li>
                            <li><a href="{{ path('app_upload_file') }}"><span class="glyphicon glyphicon-plus"></span> Ajouter</a></li>
                            <li><a href="{{ path('app_share_home') }}"><span class="glyphicon glyphicon-random"></span> Partage</a></li>
                            {% if app.user.roles[0] == "ROLE_ADMIN" %}
                                <li><a href="{{ path('app_admin') }}"><span class="glyphicon glyphicon-wrench"></span> Administration</a></li>
                            {% endif %}
                            <li><a href="{{ path('app_contact') }}"><span class="glyphicon glyphicon-send"></span> Contact</a></li>
                            <li>
                                <a href="{{ path('app_logout') }}" class="btn btn-default btn-outline btn-circle collapsed"  data-toggle="collapse" href="#nav-collapse4" aria-expanded="false" aria-controls="nav-collapse4"><span class="glyphicon glyphicon-off"></span> Déconnexion <i class=""></i> </a>
                            </li>
                        {% else %}
                            <li>
                                <a href="{{ path('app_login') }}" class="btn btn-default btn-outline btn-circle collapsed"  data-toggle="collapse" href="#nav-collapse4" aria-expanded="false" aria-controls="nav-collapse4"><span class="glyphicon glyphicon-arrow-right"></span> Connexion <i class=""></i> </a>
                            </li>
                            <li>
                                <a href="{{ path('app_signup') }}" class="btn btn-default btn-outline btn-circle collapsed"  data-toggle="collapse" href="#nav-collapse4" aria-expanded="false" aria-controls="nav-collapse4"><span class="glyphicon glyphicon-share-alt"></span> Inscription <i class=""></i> </a>
                            </li>
                        {% endif %}
                    </ul>
                </div>
            </div>
        </nav>
    </div>
{% endblock %}
{% block body %}
{% endblock %}
{% block javascripts %}
{% endblock %}
</body>
</html>
