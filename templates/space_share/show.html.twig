{% extends "base.html.twig" %}
{% block body %}
    <div class="row justify-content-center">
        <div class="col=12 mt-3">
            <div class="jumbotron ">
                <h1 class="display-4">Espace partagés</h1>
                <p class="lead">Partagez votre espace avec vos amis</p>
                <hr class="my-4">
                <p>Vous pouvez consultez les dossiers que l'on partage avec vous, ceux que vous partagez ou créer une relation avec un dossier d'un ami</p>
                <p class="lead">
                    <a class="btn btn-primary btn-lg" href="{{ path('app_show_specific_space', { 'main': "true" }) }}" role="button">Qui peux accéder à mon espace ?</a>
                    <a class="btn btn-primary btn-lg" href="{{ path('app_show_specific_space', { 'main': "false" }) }}" role="button">A quelle espace ai-je accès ?</a>
                    <a class="btn btn-primary btn-lg" href="{{ path('app_create_share_spaces') }}" role="button">Créer une relation</a>
                </p>
            </div>
        </div>
    </div>
{% endblock %}


